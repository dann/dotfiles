For Ubuntu

List installed packages
  dpkg --get-selections | grep '[[:space:]]install$='| awk '{print $1}' > deb-packages 

Install listed packages
  cat deb-packages | xargs sudo aptitude install

For Mac

