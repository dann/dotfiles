Download
  
    % svn co http://svn.lazy-people.org/member/oreore/dann/dotfiles

Installation
  zshにシェルを変更して、
 
    % cd ~/project/dotfiles 
    % ./setup.sh

How to customize configs
  
  zshrcのカスタマイズ方法
     
     ~/.zshr-afterまた~/.zshrc-beforeを作成
     それらのファイルに自分の設定を書くことで、それが有効になる。

  vimrcのカスタマイズ方法
     
     ~/.vimrc-after, ~/.vimrc-beforeを用意する
     それらのファイルに自分の設定を書くことで、それが有効になる。

How to use

   screen sessionの共有
      screen -x username/

      usernameは、.dev_users中のユーザー名
